import React, { Component } from 'react'

export default class Saved extends Component {
  render() {
    return (
      <div style={styles.body}>
        Saved
      </div>
    )
  }
}

const styles = {
  body: {
    margin: '0 auto',
    maxWidth: 500,
    marginTop: 64,
    marginBottom: 80
  }
}
